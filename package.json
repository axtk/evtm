{
  "name": "@axtk/event-manager",
  "version": "1.2.2",
  "description": "A lightweight event manager",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild dist/index.js --bundle --outfile=index.build.js --global-name=EventManager",
    "build:min": "npm run build -- --minify",
    "test": "node -r esm dist/test.js",
    "tsc": "tsc"
  },
  "author": "axtk",
  "license": "MIT",
  "keywords": [
    "events",
    "event-manager",
    "event-emitter",
    "dispatcher",
    "mediator"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/axtk/event-manager.git"
  },
  "devDependencies": {
    "esbuild": "^0.11.2",
    "esm": "^3.2.25",
    "typescript": "^4.2.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
